config:
  path: {{:full.path/here}}
  longer-path: {{[:another :full :path :here]}}
  version: {{:version}}
  postgres:
    user: {{ [:infra [:env] :postgres :user] }}
    user: {{ [:infra [:env] :postgres :password] }}
